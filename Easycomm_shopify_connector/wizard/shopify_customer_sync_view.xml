<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Sync Customers to Shopify wizard -->
    <record id="view_shopify_customer_sync_wizard_form" model="ir.ui.view">
        <field name="name">shopify.customer.sync.wizard.form</field>
        <field name="model">shopify.customer.sync.wizard</field>
        <field name="arch" type="xml">
            <form string="Sync to Shopify">
                <sheet>
                    <div class="alert alert-info" role="alert">
                        Choose the Shopify store (instance) to create/update the selected customer(s) in.
                        New customers are created in Shopify; already-linked ones get their details updated.
                    </div>
                    <group>
                        <field name="shopify_instance_id"
                               options="{'no_create': True, 'no_open': True}"/>
                        <field name="customer_count"/>
                    </group>
                    <field name="partner_ids">
                        <list>
                            <field name="name"/>
                            <field name="email"/>
                            <field name="phone"/>
                            <field name="shopify_customer_id"/>
                            <field name="shopify_instance_id"/>
                        </list>
                    </field>
                </sheet>
                <footer>
                    <button name="action_sync" type="object" string="Sync to Shopify" class="oe_highlight"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action (also bound to the contacts list "Action" menu) -->
    <record id="action_shopify_customer_sync_wizard" model="ir.actions.act_window">
        <field name="name">Sync to Shopify</field>
        <field name="res_model">shopify.customer.sync.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="binding_view_types">list,form</field>
    </record>
</odoo>
